# PageRank Directed Graph Computer

A comprehensive Rust library for computing PageRank scores over a directed graph
of attestations.

## Building

Install wasm-pack

```bash
cargo install wasm-pack
```

Build the WASM packed for the web into the `pkg` directory:

```bash
wasm-pack build --target web
```
