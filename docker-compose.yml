###################################
#
# make start-all-local
#
###################################

# docker volume rm $(docker volume ls -q -f "dangling=true")

services:
  warg-server:
    image: "ghcr.io/reecepbcups/warg-registry:v0.9.3"
    container_name: "warg-server"
    platform: linux/amd64
    environment:
      WARG_OPERATOR_KEY: ecdsa-p256:I+UlDo0HxyBBFeelhPPWmD+LnklOpqZDkrFP5VduASk=
      WARG_NAMESPACE: example
      WKG_REGISTRY: http://localhost:5000
      WARG_CONTENT_BASE_URL: http://localhost:8090
      WARG_LISTEN: 0.0.0.0:8090
      WARG_VERBOSE: 1
    ports:
      - 8090:8090
    command: ["--rm"]

  ipfs:
    image: ipfs/kubo:v0.37.0
    container_name: ipfs
    network_mode: host
    ports:
      - "4001:4001"
      - "4001:4001/udp"
      - "8080:8080"
      - "5001:5001"
    stop_signal: SIGKILL
    command: daemon
    restart: unless-stopped

  # ollama:
  #   image: ollama/ollama:0.11.0
  #   container_name: "ollama"
  #   stop_signal: SIGKILL
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama-data:/root/.ollama
  #   entrypoint: ["/bin/sh", "-c"]
  #   command:
  #     - |
  #       ollama serve &
  #       sleep 5
  #       ollama pull llama3.2
  #       wait
  #   restart: unless-stopped

  merkler-events-server:
    image: halverneus/static-file-server:latest
    ports:
      - "9090:9090"
    volumes:
      - ./infra/wavs-1/app:/web
    environment:
      - PORT=9090
      # Optional config, e.g. CORS or TLS

  ponder-db:
    image: postgres:17
    container_name: ponder-db
    environment:
      POSTGRES_DB: ponder
      POSTGRES_USER: ponder
      POSTGRES_PASSWORD: ponder
    volumes:
      - ponder-db-data:/var/lib/postgresql/data
    ports:
      - "6432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d ponder -U ponder"]
      interval: 1s
      timeout: 3s
      retries: 5

  # ponder:
  #   image: node:22-alpine
  #   container_name: ponder
  #   working_dir: /app/ponder
  #   network_mode: host
  #   volumes:
  #     - .:/app
  #   command: sh -c "npm install && npm run db:migrate && npm run start"
  #   depends_on:
  #     - ponder-db
  #   ports:
  #     - "42069:42069"
  #   environment:
  #     DATABASE_URL: postgresql://ponder:ponder@localhost:6432/ponder
  #   restart: unless-stopped

volumes:
  ollama-data:
  ponder-db-data:
